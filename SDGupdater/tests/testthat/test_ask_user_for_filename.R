context("Calculate rate per 1000 when numerator is > 3")


test_that("calculate_valid_rates_per_1000 stops with appropriate errors when arguments are invalid") # test on a vestor and on a tibble

test_that("calculate_valid_rates_per_1000 produces the correct value")

test_that("calculate_valid_rates_per_1000 returns NA when numerator is <= 3")

